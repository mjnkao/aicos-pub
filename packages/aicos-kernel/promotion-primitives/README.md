# Promotion Primitives

Status: initial MVP scaffold

The MVP primitive is review-packet creation. It copies a source artifact into
`serving/promotion/review-packets/<lane>/` and marks it pending human review.

It does not mutate canonical truth directly.

