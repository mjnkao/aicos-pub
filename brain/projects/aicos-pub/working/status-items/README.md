# aicos-pub Status Items

This directory stores public-safe status items for `projects/aicos-pub`.

Agents may add MCP-written status updates here when their bearer token policy
allows writes to `projects/aicos-pub`.
