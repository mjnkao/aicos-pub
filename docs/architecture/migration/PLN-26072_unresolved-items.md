# PLN-26072 Unresolved Items

Status: initial unresolved list

- Select stable company and workspace identifiers after review.
- Decide which old canonical files should migrate first.
- Decide whether full JSON Schema validation should be added as a dependency or
  kept stdlib-only for now.
- Define the stable MCP stdio entrypoint after the local CLI proof works.
- Decide how much of old `gateway/` should be retained versus replaced by the
  new kernel/serving split.
- Decide when to split
  `docs/New design/RUL-26088_AICOS_Temporary_Role_Rules_For_Codex.md` into
  permanent class-specific rule files. Current decision: defer splitting and
  keep the important A1/A2 role model surfaced in `AGENTS.md` until rules are
  more stable.
