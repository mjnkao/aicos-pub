# Validators

Status: initial MVP scaffold

The local `aicos validate` commands currently perform deterministic required
field and required file checks. Full JSON Schema validation can be added later
without changing packet shapes.

