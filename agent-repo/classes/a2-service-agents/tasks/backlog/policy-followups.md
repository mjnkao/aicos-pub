# Policy Followups

Status: backlog

## Items

- Decide when `aicos sync brain` should run embedding/vector refresh by default.
- Decide when `aicos option choose` needs actor registry, review signature, or
  reason taxonomy.
- Add fuller JSON Schema validation after packet shapes stabilize.
- Decide when to split temporary role rules into permanent class-specific files.
- Define the promotion path for policy changes from source docs to canonical
  rules after repeated use.
- Improve `./aicos context start` packet list with one-line summaries for each
  task packet.
- Decide whether/how to install or expose `aicos` on PATH; current reliable
  repo-local command is `./aicos`.
- Tighten `context start` suggested next reads so the output stays minimal for
  fresh-thread startup.
- Decide when task packet selection metadata should become richer than file
  names plus packet body.
- Extract shared operating policy concepts for future A1 cards without copying
  A2-Core taxonomy or A2 service lanes.
- Evaluate thin DB-backed registries for packet metadata, handoff metadata,
  queues, candidate decisions, and sync ledger after Markdown authority patterns
  stabilize.
- Refine blocked/waiting task-state conventions after more real handoff-ready
  task metadata examples exist.
- Create a staged discussion checklist for AICOS information architecture so the
  team can first enumerate issues, then prioritize them, then go deep item by
  item instead of jumping early into one design problem.
- Define candidate design directions for modular markdown-first brain structure,
  multi-resolution reading views, and project-specific information architecture
  profiles.
- Decide what should be standardized as soft grammar/primitives versus left to
  project-specific overlays.

## Source

Normalized from the 2026-04-18 policy pack:

- `RUL-26088`
- `A2S-26102`
- `FLW-26124`
- `POL-26140`
- `POL-26145`
- `POL-26150`
- `HMD-26210`
