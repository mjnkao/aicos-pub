# Agents PM Dashboard Alias Context Ladder

Scope: `projects/agents-pm-dashboard`
Canonical scope: `projects/agents-dashboard`

## First Step

Switch to `scope=projects/agents-dashboard`.

## Why

The AICOS project was created as `agents-dashboard`, while the local folder is
`agents-pm-dashboard`.
