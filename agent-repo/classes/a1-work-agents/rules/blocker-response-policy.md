# A1 Blocker Response Policy

Status: reference/deep policy, not startup-default

For a blocker:

1. state the blocker;
2. generate at least two options when feasible;
3. recommend one reversible MVP path;
4. write an option packet for human review.

